============
Installation
============

At the command line::

    $ easy_install pyunitgrading

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv pyunitgrading
    $ pip install pyunitgrading
